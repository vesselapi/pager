const TeamsPage = () => {
  // const schedules = await
  return (
    <div>
      {/* Sort + Add Team */}
      {/* Headers for teams list */}
      {/* Teams list goes here */}
    </div>
  );
};

export default TeamsPage;
